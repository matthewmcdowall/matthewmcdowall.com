version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # One PR for routine bumps; security advisories still get their own PR.
      minor-and-patch:
        update-types: [minor, patch]
